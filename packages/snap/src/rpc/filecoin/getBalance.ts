import {Wallet} from "../../interfaces";

/**
 * Returns balance as BN
 * @param wallet
 * @param api
 * @param address
 */
export async function getBalance(wallet: Wallet, api: any, address?: string): Promise<string> {
  return 'balance';
}